/**
 * Donor registration page — teal accent route.
 *
 * URL: /donate
 * Renders the DonorForm component for new blood donor signups.
 */
import DonorForm from "@/components/DonorForm";

export default function DonatePage() {
  return <DonorForm />;
}
